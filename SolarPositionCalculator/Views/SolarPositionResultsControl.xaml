<UserControl x:Class="SolarPositionCalculator.Views.SolarPositionResultsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">
    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Padding="10">
        <StackPanel>
            <TextBlock Text="Solar Position Results" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
            
            <!-- Calculate Button -->
            <Button Content="Calculate Solar Position" 
                    Command="{Binding CalculateSolarPositionCommand}"
                    Margin="0,0,0,10"
                    Padding="8,4"
                    Background="LightBlue"/>
            
            <!-- Loading Indicator -->
            <TextBlock Text="Calculating..." 
                       FontStyle="Italic"
                       Margin="0,0,0,10"/>
            
            <!-- Results Display -->
            <TextBlock Text="{Binding FormattedSolarPosition}" 
                       FontFamily="Consolas"
                       Background="LightGray"
                       Padding="5"
                       Margin="0,0,0,10"
                       TextWrapping="Wrap"/>
            
            <!-- Sun Visibility Status -->
            <TextBlock Text="{Binding SunVisibilityStatus}" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"/>
            
            <!-- Status Message -->
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="11"
                       Foreground="DarkGreen"
                       TextWrapping="Wrap"
                       Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</UserControl>